import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    headerText: {
        fontSize: 20,
        marginTop: 10,
        marginBottom: 10
    },
    contentText: {
        fontSize: 16
    }
});
